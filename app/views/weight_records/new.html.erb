<h1>今日の体重を記録</h1>

<% if @weight_record.errors.any? %>
  <div>
    <ul>
      <% @weight_record.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>

<%= form_with model: @weight_record do |form| %>
  <div>
    <%= form.label :weight, "今日の体重" %>
    <%= form.number_field :weight, step: 0.1, min: 0.1 %>
    <span>kg</span>
  </div>

  <div>
    <%= form.submit "記録する" %>
  </div>
<% end %>
